export enum Priority {
  one = 1,
  two = 2,
  three = 3,
  four = 4
}

export enum Status {
  notStarted = 0,
  inProgress = 1,
  block = 2,
  done = 3
}

export enum HttpStatusCode {
  OK = 200,
  BAD_REQUEST = 400,
  NOT_FOUND = 404,
  INTERNAL_SERVER = 500,
}
